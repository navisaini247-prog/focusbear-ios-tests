#!/usr/bin/env node
/**
 * Tunnel registry entry for an Apple TV (WiFi) device.
 */
export type AppleTVRegistryEntry = {
    udid: string;
    address: string;
    rsdPort: number;
    packetStreamPort: number;
};
/**
 * Tunnel connection returned from TunnelManager.getTunnel for an Apple TV (WiFi) socket.
 */
export type AppleTVTunnelConnection = {
    Address: string;
    RsdPort?: number | undefined;
    addPacketConsumer?: ((c: unknown) => void) | undefined;
    closer?: (() => Promise<void>) | undefined;
};
/**
 * Input for teardown of an Apple TV tunnel (full or partially created). All fields may be null if not yet created.
 */
export type AppleTVTunnelTeardownInput = {
    tunnel?: AppleTVTunnelConnection | null | undefined;
    packetStreamServer?: PacketStreamServer | null | undefined;
    tunnelService?: AppleTVTunnelService | null | undefined;
    tlsSocket?: import("node:tls").TLSSocket | null | undefined;
};
/**
 * Resource handle for cleanup of a single Apple TV (WiFi) tunnel.
 */
export type AppleTVTunnelResource = {
    packetStreamServer: import("appium-ios-remotexpc").PacketStreamServer;
    tunnelService: import("appium-ios-remotexpc").AppleTVTunnelService;
    udid: string;
    tunnel: AppleTVTunnelConnection;
    tlsSocket: import("node:tls").TLSSocket;
};
import { PacketStreamServer } from 'appium-ios-remotexpc';
import { AppleTVTunnelService } from 'appium-ios-remotexpc';
//# sourceMappingURL=tunnel-creation.d.mts.map