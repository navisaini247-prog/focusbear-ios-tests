<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_root"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:background="@drawable/widget_background_dark"
    android:elevation="4dp"
    android:padding="0dp">

    <LinearLayout
        android:id="@+id/widget_header"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:background="@drawable/widget_background_orange"
        android:padding="12dp"
        android:gravity="center_vertical"
        android:minHeight="70dp">

        <ImageView
            android:id="@+id/widget_icon"
            android:layout_width="36dp"
            android:layout_height="36dp"
            android:scaleType="fitCenter"
            android:src="@drawable/focus_bear_icon"
            android:contentDescription="@string/widget_focus_bear_logo"
            android:layout_marginEnd="10dp" />

        <TextView
            android:id="@+id/widget_blocking_reason"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="@string/widget_no_reason_provided"
            android:textColor="@color/widget_header_text"
            android:textSize="16sp"
            android:textStyle="bold"
            android:maxLines="2"
            android:ellipsize="end" />
    </LinearLayout>


    <LinearLayout
        android:id="@+id/widget_next_habit"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:gravity="center"
        android:padding="16dp">

        <TextView
            android:id="@+id/widget_next_habit_name"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="center"
            android:text="@string/widget_default_routine"
            android:textColor="@color/widget_text"
            android:textSize="16sp"
            android:padding="12dp"
            android:minHeight="48dp"
            android:maxLines="2"
            android:ellipsize="end"/>
    </LinearLayout>
    
</LinearLayout>
