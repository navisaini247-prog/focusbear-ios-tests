export const isPostPoneFlowFromDistractionAlertSelector = (state) => state.activity.isPostPoneFlowFromDistractionAlert;

export const isBlockingSelector = (state) => state.activity.isBlocking;

export const eventQueueSelector = (state) => state.activity.eventQueue;
export const isSendingEventsSelector = (state) => state.activity.isSendingEvents;
